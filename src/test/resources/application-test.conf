ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.dubcast.bff.ApplicationKt.module ]
    }
}

dubcast {
    storage {
        basePath = "./build/test-storage"
    }
    baseUrl = "http://localhost:8080"
}
