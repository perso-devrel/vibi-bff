# vibi-bff — required env vars. copy to .env 후 모두 채우기.
# 옵션 변수는 application.conf default 사용 — 필요 시 .env 에 직접 추가.
#
# 이 파일의 용도: 로컬 dev (`./gradlew run`) + deploy/cloud-run.sh 부트스트랩 1회.
# 운영 (Cloud Run) env 의 단일 소스는 .github/workflows/deploy.yml 의 declarative wiring:
#   - GitHub Secrets:   GOOGLE_OAUTH_CLIENT_IDS, GOOGLE_OAUTH_CLIENT_ID_ADMIN (Vite 빌드 인라인용),
#                       APPLE_OAUTH_CLIENT_IDS, ADMIN_SLUG,
#                       IAP_APPLE_ISSUER_ID, IAP_APPLE_KEY_ID, IAP_APPLE_PRIVATE_KEY,
#                       GCP_WIF_PROVIDER, GCP_SA_EMAIL, GCP_PROJECT_ID, GCP_RUNTIME_SA_EMAIL
#   - GitHub Variables: CORS_ALLOWED_ORIGINS, BFF_BASE_URL (커스텀 도메인 — 미설정 시 자동 self-ref),
#                       R2_BUCKET, R2_ACCOUNT_ID,
#                       IAP_APPLE_BUNDLE_ID, IAP_APPLE_ENV, IAP_GOOGLE_PACKAGE_NAME
#   - Secret Manager:   PERSO_API_KEY, PERSO_SPACE_SEQ, AUTH_JWT_SECRET,
#                       SEPARATION_SIGNING_SECRET, DATABASE_URL, DB_USER, DB_PASSWORD,
#                       R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY
#                       (Android 결제 실연동 시 +IAP_GOOGLE_SERVICE_ACCOUNT_JSON)
# 즉 운영 갱신은 GitHub repo Settings → Secrets and variables → Actions 에서. 그 후 push 또는 Run workflow.

# 콤마 분리 Google OAuth client IDs (iOS / Android / Web)
GOOGLE_OAUTH_CLIENT_IDS=

# 콤마 분리 Apple Sign In client IDs (보통 iOS bundle id `com.vibi.ios`).
# 비워두면 Apple 로그인 비활성 — /auth/apple 호출 시 503 apple_signin_disabled.
APPLE_OAUTH_CLIENT_IDS=

# Admin UI (Vite) 빌드 타임에 번들에 인라인되는 Google OAuth web client ID.
# 보통 GOOGLE_OAUTH_CLIENT_IDS 의 web client (Authorized JS origins 에 BFF_BASE_URL 등록된 것)
# 와 동일. 누락 시 admin LoginPage 가 "빌드 env 가 없습니다" 표시 + Google 버튼 안 뜸.
# 운영에선 GitHub Secret `GOOGLE_OAUTH_CLIENT_ID_ADMIN` 으로 주입 (deploy.yml 의 admin-ui 빌드 step).
VITE_GOOGLE_OAUTH_CLIENT_ID_ADMIN=

# Admin UI 마운트 경로 — `/${ADMIN_SLUG}/` 로 Ktor staticResources 가 서빙. URL 추측 방지용 임의 slug.
ADMIN_SLUG=

# HMAC-SHA256 키, 각각 32+ chars. openssl rand -hex 32
AUTH_JWT_SECRET=
SEPARATION_SIGNING_SECRET=

# Perso AI
PERSO_API_KEY=
PERSO_SPACE_SEQ=

# ── 인앱결제(IAP) 영수증 검증 — 모두 옵션. 비우면 해당 플랫폼 결제 비활성 ─────────────────
# (purchase 시 iap_unconfigured 400. 검증 로직은 service/iap/*ReceiptVerifier.kt)
# Apple(StoreKit2): 아래 5개를 모두 채워야 활성화 (하나라도 비면 Apple 결제 전체 비활성).
#   값 출처: App Store Connect → Users and Access → Integrations(Keys) — Issuer ID / Key ID / .p8.
#   IAP_APPLE_PRIVATE_KEY 는 .p8 파일 본문(PEM). 한 줄 env 로 넣을 땐 개행을 \n literal 로 박을 것
#   (코드가 \n → 실제 개행 복원). 운영(Cloud Run)은 GitHub Secret 으로 주입 — deploy.yml 배선.
IAP_APPLE_ISSUER_ID=
IAP_APPLE_KEY_ID=
IAP_APPLE_PRIVATE_KEY=
IAP_APPLE_BUNDLE_ID=
IAP_APPLE_ENV=
# Google(Play Billing, Android): 모바일이 아직 mock 이라 보류. 실연동 시 packageName +
#   service account JSON 필요. JSON 은 client_email 의 '@' 가 Cloud Run env 구분자(^@^)와 충돌 +
#   비밀이라 운영에선 env 가 아니라 Secret Manager(IAP_GOOGLE_SERVICE_ACCOUNT_JSON)로 주입.
IAP_GOOGLE_PACKAGE_NAME=
IAP_GOOGLE_SERVICE_ACCOUNT_JSON=

# dev 전용 surface 노출 토글 — 운영에선 비워둘 것(기본 비활성).
#   ENABLE_SWAGGER=true        → /swagger API 문서 UI 마운트 (무인증 노출이라 운영 OFF)
#   ENABLE_TESTDATA_MOCK=true  → /api/v2/testdata/separation/* mock 라우트 마운트 (dev 전용)
ENABLE_SWAGGER=
ENABLE_TESTDATA_MOCK=

# Vertex AI (Gemini) — Vertex AI 가 활성화된 GCP project ID.
# Cloud Run 이 배포된 프로젝트와 다를 수 있어 GCP_PROJECT_ID 와 이름 분리.
GEMINI_PROJECT_ID=
# 로컬 dev 전용 SA JSON 경로. 비우면 ADC 사용 (Cloud Run 의 attached SA,
# 또는 로컬 `gcloud auth application-default login` 캐시).
GOOGLE_APPLICATION_CREDENTIALS=

# Postgres (Neon managed) — user 영속화 + 향후 IAP entitlement.
# Neon UI 의 connection string 에 jdbc: 접두사 추가, user/password 는 분리해서 입력.
# 예: jdbc:postgresql://ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require
# HikariCP pool 크기는 application.conf default 5 사용 — 튜닝 필요할 때만 DB_MAX_POOL 추가.
DATABASE_URL=
DB_USER=
DB_PASSWORD=

# Cloudflare R2 — 큰 산출물 다운로드를 SigV4 presigned URL redirect 로 우회.
# R2 egress 무료라 Cloud Run egress 비용 0. blank 면 respondFile streaming fallback (dev 호환).
# 발급: Cloudflare dashboard → R2 → Manage API Tokens → Object Read & Write 권한.
# R2_ACCOUNT_ID 는 dashboard URL (`dash.cloudflare.com/<id>/r2`) 의 32자 hex.
R2_BUCKET=
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=

# ── 출시 보강 튜닝 (모두 옵션 — 비우면 코드 default) ───────────────────────────
# 레이트리밋 신뢰 프록시 hop 수 — X-Forwarded-For 의 *오른쪽에서* 이만큼 건너뛴 위치를
# 실제 클라 IP 로 본다. leftmost 는 클라가 위조 가능해 가입 보너스 크레딧 파밍 우회가 되므로,
# Cloud Run 처럼 LB 가 신뢰 IP 를 오른쪽에 append 하는 환경에선 반드시 설정 권장.
#   - 비움/0 = 기존 동작(leftmost, 위조 가능) — 하위호환 default.
#   - Cloud Run = 보통 1 (맨 오른쪽 GFE hop 을 건너뛴 그 앞이 신뢰 클라 IP). 실제 XFF 를
#     스테이징에서 한 번 로깅해 hop 수를 확인 후 확정할 것 (과다 설정 시 프록시 IP 공유로 과차단).
# 운영(Cloud Run) 은 GitHub Variable `RATE_LIMIT_TRUSTED_PROXY_HOPS` 로 주입 — deploy.yml 배선.
RATE_LIMIT_TRUSTED_PROXY_HOPS=

# uploads/ 원본 미디어(개인정보) 보존 TTL(시간). 정상 경로는 업로드 직후 삭제하므로 잔존분만
# 대상. 방침의 보존기간(72h)보다 짧게 유지. 비우면 기본 6.
UPLOADS_TTL_HOURS=

# admin-grant 1인당 24h 롤링 적립 상한. 유출 admin JWT 의 무한 적립 차단. 비우면 기본 1000.
ADMIN_GRANT_DAILY_CAP=

# 최종 ffmpeg 패스 1회의 hard timeout(분). 무한 정지 시 동시성 permit 영구 누수 차단.
# 아주 긴 영상의 최종 mix 가 이 시간을 넘길 수 있으면 상향. 비우면 기본 30.
RENDER_PROCESS_TIMEOUT_MIN=
